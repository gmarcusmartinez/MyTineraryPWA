import React from 'react'

const CityCreatorCard = ({ name }) => {
  return (
    <div className="card">
      <p className="card-title red-text text-lighten-2">{name}</p>
    </div>
  )
}

export default CityCreatorCard
